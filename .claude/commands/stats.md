---
description: Funnel table and key metrics, split by channel.
---
Show the pipeline funnel and metrics:
```
uv run cli stats
```
For a single channel: `uv run cli stats --channel linkedin` or `--channel post`.
Highlight where the funnel is leaking and the reply/claim rates.
